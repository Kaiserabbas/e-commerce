<!DOCTYPE html>
<html>
  <head>
    <title>ECommerce</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "inter-font", "data-turbo-track": "reload" %>
   <%= javascript_importmap_tags %>
   
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex">
      <nav class="flex w-full">
        <div class="w-64 p-4 h-screen bg-grey-600 sticky top-0">
            <div class="flex">
                <%= image_tag "logo.png", class: "w-4" %>
                <h1 class="text-md py-2 text-grey-50 font-bold hidden md:block">ECommerce Admin</h1>
            </div>
        </div>  
 
      </nav>
      <%= yield %>
    </main>
  </body>
</html>
