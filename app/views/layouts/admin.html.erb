<!DOCTYPE html>
<html>
  <head>
    <title>ECommerce</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "inter-font", "data-turbo-track": "reload" %>
   <%= javascript_importmap_tags %>
       <style>
      .grey {
        background-color: #d2d2d2;
      }
    </style>
  </head>

  <body>
    <main class="flex">
      <nav class="flex w-full">
        <div class="grey w-64 p-4 h-screen sticky top-0">
            <div class="flex flex-col items-center justify-center text-center ">
                <%= image_tag "logo.png", class: "w-32 h-auto rounded" %>
                <h2 class="text-md py-2 hidden md:block"><strong class="font-serif">چاند ٹیلر</strong> Admin</h2>
            </div>
            <ul class="flex flex-col text-white bg-gray-700">
                <li>
                    <%= link_to admin_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                    <%= icon('fa-solid', 'desktop', class: 'mr-2') %> <span class="hidden md:inline-block">Dashboard</span>
                  <% end %>
                </li>
                <li>
                    <%= link_to admin_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                    <%= icon('fa-solid', 'cart-shopping', class: 'mr-2') %> <span class="hidden md:inline-block">Products</span>
                  <% end %>
                </li>
                <li>
                  <%= link_to admin_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                    <%= icon('fa-solid', 'list', class: 'mr-2') %> <span class="hidden md:inline-block">Categories</span>
                  <% end %>
                </li>
                <li>
                  <%= link_to admin_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                    <%= icon('fa-solid', 'truck-fast', class: 'mr-2') %> <span class="hidden md:inline-block">Orders</span>
                  <% end %>
                </li>
                <li>
                  <%= link_to destroy_admin_session_path, data: { "turbo-method": :delete, "turbo-confirm": "Are you sure you want to sign out?" }, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                    <%= icon('fa-solid', 'sign-out', class: 'mr-2') %> <span class="hidden md:inline-block">Sign out</span>
                  <% end %>
                </li>
            </ul>
        </div>  
      </nav>
      <%= yield %>
    </main>
  </body>
</html>
